#ifndef ASSEMBLER_H
#define ASSEMBLER_H

#include "global.h"
#define MAX 200

void processaQuadruplas(); // Percorre a árvore e gera código intermediáriovoid imprimirQuadruplas();    // Imprime o código intermediário geradovoid printTTabela();tempTab** criarTempTabela();void adicionarQuadruplas(const char* formato, ...);

#endif